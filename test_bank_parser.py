from utils.bank_parser import BankStatementParser

# Replace with your actual PDF path & password (or None if no password)
parser = BankStatementParser("data/Statement.pdf", password="GAUR1903")
print(parser.detect_bank_from_ pdf())

